import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    width: '100%',
    borderRadius: 50,
    borderWidth: 1,
    paddingHorizontal: 25,
    paddingVertical: 15,
  },
  text: {
    fontSize: 18,
  },
})
