import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  modal: {
    width: '80%',
    padding: 10,
    alignItems: 'center',
    backgroundColor: 'gray',
  },
  title: {
    paddingVertical: 5,
  },
  errorStack: {
    paddingTop: 10,
  },
})
